<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Purchase Invoice Detail</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
</head>
<body>
    <div class="container">
        <a href="/chitiethoadonnhap"><h3>Purchase Invoice Detail Page</h3></a>
        <h2>UPDATE PURCHASE INVOICE DETAIL</h2>
        <form action="/chitiethoadonnhap/<%= purchaseInvoiceDetail._id %>" method="POST">
            <div class="form-group">
                <label>Purchase Invoice Detail ID</label>
                <input class="form-control" type="text" name='MaHDN' value="<%= purchaseInvoiceDetail.MaHDN %>">
            </div>
            <div class="form-group">
                <label>Product ID</label>
                <input class="form-control" type="text" name='MaSP' value="<%= purchaseInvoiceDetail.MaSP %>">
            </div>
            <div class="form-group">
                <label>Quantity</label>
                <input class="form-control" type="number" name='SoLuongNhap' value="<%= purchaseInvoiceDetail.SoLuongNhap %>">
            </div>
            <div class="form-group">
                <label>Price</label>
                <input class="form-control" type="number" name='DonGiaNhap' value="<%= purchaseInvoiceDetail.DonGiaNhap %>">
            </div>
            <input id="update-btn" class="btn btn-primary" type="submit" value="SAVE">
            <input id="delete-btn" class="btn btn-secondary" type="button" value="DELETE"
                onclick="handleDeletePurchaseInvoiceDetail('<%= purchaseInvoiceDetail._id %>')">
        </form>
    </div>
    <script>
        function handleDeletePurchaseInvoiceDetail(value) {
            $.ajax({
                url: '/chitiethoadonnhap/' + value,
                type: 'DELETE',
                success: function (result) {
                    $(location).attr('href', '/');
                }
            })
        }
    </script>
</body>
</html>
