<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orders</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
</head>
<body>
    <div class="container">
        <h2>Order Manager</h2>
        <a href="donhang/add-order" class="btn btn-success float-right">Add Order</a>
        <table class="table table-hover">
            <thead>
                <tr>
                    <!-- <th>Order ID</th> -->
                    <th>Customer ID</th>
                    <th>Product ID</th>
                    <th>Product</th>
                    <th>Color</th>
                    <th>Size</th>
                    <th>Material</th>
                    <th>Selling Price</th>
                    <th>Date</th>
                    <th>Total Amount</th>
                    <!-- <th>Status</th> -->
                    <th>Edit</th>
                </tr>
            </thead>
            <% orders.forEach((entry) => { %>
            <tr ondblclick="handleUpdateOrder('<%= entry.MaKH %>')">
                <!-- <td><%= entry.MaDonHang %></td> -->
                <td><%= entry.MaKH %></td>
                <td><%= entry.MaSP %></td>
                <td><%= entry.TenSP %></td>
                <td><%= entry.Mau %></td>
                <td><%= entry.Size %></td>
                <td><%= entry.ChatLieu %></td>
                <td><%= entry.GiaBan %></td>
                <td><%= entry.NgayLap %></td>
                <td><%= entry.ThanhTien %></td>
                <!-- <td><%= entry.TrangThai %></td> -->
                <td>
                    <input class="btn btn-primary" type="button" value="Update"
                        onclick="handleUpdateOrder('<%= entry.MaKH %>')">
                </td>
            </tr>
            <% }) %>
        </table>
        <script>
            function handleUpdateOrder(value) {
                $(location).attr('href', 'donhang/update/' + value);
            }
        </script>
    </div>
</body>
</html>
